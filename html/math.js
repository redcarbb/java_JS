export const add = (a, b) => a + b;
export const subtract = (a, b) => a - b;

const hello = (x) => `hello ${x}`;
const version = "V.202250912.1";
const utils = {
  add,
  subtract,
  hello,
  version,
};
export default utils;
